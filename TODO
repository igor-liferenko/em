make second argument to be the line number to be shown when file is opened

move each #include to the section where it is needed

add C-r for backward search

via xkb patch use BackSpace to insert "leftarrow" (to set value to a variable) and "not_equal" (to check for inequality) characters from extended character set for CWEB,
and use = in its normal meaning of checking for equality, and uncomment "\let\K=\leftarrow" in cwebmaX.tex
