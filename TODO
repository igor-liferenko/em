make second argument to be the line number to be shown when file is opened

move each #include to the section where it is needed

add C-r for backward search

via xkb patch use [ with Alt modifier to insert substitute for <= (≤) and ] with
Alt modifier to insert substitute for >= (≥); and uncomment "\let\K=\leftarrow" in cwebmaX.tex
do not use substitute for && and || because substitute for || looks like v, and it is better to use non of them than only one of them, because they constitute a pair
